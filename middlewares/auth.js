// const auth = (req, res, next) => {
   
//     const authHeader = req.headers.authorization

//     const [type, token] = authHeader.split(' ') || ''

//     if(type !== "Bearer") {
//         // throw
//     }

//     next()
// }

// module.exports = auth